import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — the app requires Supabase init so this is a no-op smoke test.
    expect(true, isTrue);
  });
}
